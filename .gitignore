# Dependencies (not needed to run the game — three.js loads from a CDN)
node_modules/

# Local scratch / diagnostic files
_m.*
*.tmp

# OS / editor
.DS_Store
Thumbs.db

# Scratch / packaging
github.zip
_restart-server.ps1

# Full-length fire-ambience download (only the sliced loop in assets/ is used)
assets/fire-ambience-full.mp3
